"""Dataset application layer for ZED2i recording, inspection, and export.

This package groups dataset models, ports, reports, and services.

Import objects directly from their submodules to avoid eager imports and
circular dependencies.

Examples:
    from tools_zed2i.application.dataset.models import DatasetRecordingConfig
    from tools_zed2i.application.dataset.services import DatasetExporter
    from tools_zed2i.application.dataset.ports import DatasetSampleWriter
"""

__all__: list[str] = []
