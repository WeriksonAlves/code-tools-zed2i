"""Command-line entry points for tools_zed2i.

This package contains lightweight CLI modules registered as ROS 2 console
scripts. Each module should only parse command-line arguments, call application
services, and print user-facing output.
"""
